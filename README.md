# 🤖 GitHub 每日自动提交

这是一个使用 GitHub Actions 实现的每日自动提交系统，让你的 GitHub 贡献图每天都保持绿色！

## ✨ 功能特性

- 🕐 **每日定时执行**：北京时间每天 08:00 自动运行
- ⚙️ **简单配置**：直接修改配置文件即可
- 📝 **提交日志记录**：自动记录每次提交的时间
- 🖱️ **手动触发支持**：可随时手动触发测试

## 🚀 快速开始

### 第一步：修改配置文件

打开 `config.yml`，填写你的 GitHub 用户名和邮箱：

```yaml
git:
  user_name: "你的GitHub用户名"
  user_email: "你的邮箱@example.com"
```

> ⚠️ **重要**：邮箱必须是你 GitHub 账号绑定的邮箱，否则提交不会计入贡献图！

### 第二步：创建 GitHub 仓库并推送

```bash
git init
git add .
git commit -m "🎉 Initial commit - 添加每日自动提交系统"
git branch -M main
git remote add origin https://github.com/你的用户名/你的仓库名.git
git push -u origin main
```

### 第三步：启用 Actions 权限

1. 进入仓库的 **Settings** -> **Actions** -> **General**
2. 在 **Workflow permissions** 中选择 **Read and write permissions**
3. 点击 **Save** 保存

## 🧪 测试运行

你可以手动触发工作流来测试：

1. 进入仓库的 **Actions** 标签页
2. 点击左侧的 **Daily Auto Commit**
3. 点击右侧的 **Run workflow** 按钮
4. 点击绿色的 **Run workflow** 确认

## 📁 项目结构

```
commit/
├── .github/
│   └── workflows/
│       └── daily-commit.yml    # GitHub Actions 工作流配置
├── logs/
│   ├── daily-log.md            # 提交日志记录
│   └── last-commit.txt         # 最后提交时间
├── config.yml                  # ⚙️ 用户配置文件
├── .gitignore                  # Git忽略文件
└── README.md                   # 项目说明文档
```

## ⏰ 修改执行时间

如需修改执行时间，编辑 `.github/workflows/daily-commit.yml` 文件中的 cron 表达式：

```yaml
schedule:
  - cron: '0 0 * * *'  # UTC 时间，北京时间需要 +8
```

常用时间配置（均为北京时间）：
- `'0 0 * * *'` → 08:00
- `'0 1 * * *'` → 09:00
- `'0 14 * * *'` → 22:00
- `'30 2 * * *'` → 10:30

## ❓ 常见问题

### Q: 提交没有显示在我的贡献图上？
A: 请确保 `config.yml` 中的邮箱是你 GitHub 账号绑定的邮箱。

### Q: Actions 没有运行？
A: 请确保：
1. 仓库的 Actions 功能已启用
2. Workflow permissions 已设置为 Read and write

---

⭐ 如果这个项目对你有帮助，欢迎 Star！
